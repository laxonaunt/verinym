// lib.cairo
// This is the entry point for Verinym.
// We declare all our modules (files) here so they can reference each other.

pub mod issuer_registry;
pub mod credential_circuit;
pub mod proof_verifier;